{% extends 'base.html.twig' %}


{% block title %}Questions{% endblock %}{% block body %}<div class="container">
<div class="example-wrapper">

    {% if editMode %}

        <h1>Edit question
        </h1>
        {% else%}
        <h1>Créer une nouvelle question
        </h1>
    {% endif %}
</div>


{{ form_start(formQuestion)}}
<div class=" form-group">
    {{ form_row(formQuestion.question,{'attr':{'placeholder':"Question ",'class':'form-control'}}) }}
</div>


<button class="btn btn-success" type="submit">


    Valider


</button>

{{ form_end(formQuestion)}}</div></section>{% endblock %}
